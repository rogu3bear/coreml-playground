# coreml-playground

Scratchpad repo for experimenting with Core ML models, conversions, and inference.

## Notes
- Add experiments under `experiments/` (create as needed)
- Keep large model artifacts out of git; prefer Git LFS or external storage
